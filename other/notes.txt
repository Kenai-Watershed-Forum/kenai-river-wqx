# Quick Reference Commands

Stage all files and commit:
  git add -A && git commit -m 'your message here'

Refresh Quarto cache:
  quarto render --cache-refresh

Exit render early at a specific point in a script:
  knitr::knit_exit()


# Resolved Notes (archived for reference)

## resultFlag / Result Status ID approach (resolved June 2023, implemented March 2026)
General plan was to use resultFlag for data that fails QA/QC. This is now implemented
as Result Status ID = "Rejected" in the WQX export. Accepted/Rejected maps from the
binary flag column in 2021_data_flag_decisions.csv. Do not add custom FQC codes —
the existing design is complete and correct.

## ggplot fw_acute_exceed color issue (resolved March 2026)
Attempted to use color = fw_acute_exceed in ggplot; render failed.
Fixed in static_boxplot_function.R using factor(case_when(...)) for color and shape aesthetics.

## sample_condition vs sample_type column distinction (noted Feb 2023, believed resolved)
In SGS: field duplicates are labeled "PS" in sample_type.
In ALS: lab duplicates are labeled "DUP1" in sample_type.
sample_condition column holds: Field Duplicate, Trip Blank, NA.
Believed addressed in appendix_a.qmd but not confirmed — see ALS DUP task in AGENTS.md.

## 2021 QA/QC pipeline (completed March 2026)
Finish winnowing data through QA/QC steps — DONE.
Re-inspect column structure of KWF data already in EPA WQX — DONE.
Export structure for 2021 matching existing CDX columns — DONE.
2022+ data processing is handled in the separate kenai-river-wqx-qaqc repo.

## Extract scripts to .R files (noted May 2023)
Write intake/processing scripts in .R files and source them into Quarto.
Still on the task list — see AGENTS.md.


# Outstanding Items (tracked in AGENTS.md)

All unresolved items from this file have been moved to the Tasks section of AGENTS.md.
Do not track new to-do items here — use AGENTS.md instead.
